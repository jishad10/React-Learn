import React from 'react';
export const UserContext = React.createContext();

// setp1 : create context
// step2: wrap childs with context provider -> parent component e
// setp3: state access useContext hook -> data/state access korar jonno
